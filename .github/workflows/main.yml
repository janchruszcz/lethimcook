name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  api-checks:
    uses: ./.github/workflows/api-ci.yml

  frontend-checks:
    uses: ./.github/workflows/frontend-ci.yml
